import React from 'react';
import styled from "styled-components";

const Form = styled.form`
  display: flex;
  flex-direction: column;
`;

const ClockPresenter = (
    <Form>
        Clock
    </Form>
)

export default ClockPresenter;